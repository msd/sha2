SHA-256 correctness

Windows conan script

SHA-224 implementation (very easy to implement, once SHA-256 works correctly)

SHA-384 and SHA-512 

Load file in chunks (currently loads the entire file at once)
