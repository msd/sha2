Implement sha512/256
Implement sha512/224
Implement sha224
Switch arguments for printing only hash without repeating the input
Argument for taking in a file
Take stdin as input if no arguments are given or if '-' is given as a file
Separate executables for each algorithm
Switch to pick which algorithm to perform
Load file in chunks (currently loads the entire file at once)
